const router=require('express').Router();
const jwt=require('jsonwebtoken');
const User=require('../models/User');
const {sendOTP}=require('../utils/email');
const {authenticate}=require('../middleware/auth');
const genOTP=()=>Math.floor(100000+Math.random()*900000).toString();
const signToks=id=>({accessToken:jwt.sign({userId:id},process.env.JWT_SECRET,{expiresIn:'15m'}),refreshToken:jwt.sign({userId:id},process.env.JWT_REFRESH_SECRET,{expiresIn:'30d'})});
const setCookies=(res,at,rt)=>{const o={httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:process.env.NODE_ENV==='production'?'strict':'lax'};res.cookie('accessToken',at,{...o,maxAge:15*60*1000});res.cookie('refreshToken',rt,{...o,maxAge:30*24*60*60*1000});};
router.post('/signup',async(req,res)=>{try{const{username,email,password}=req.body;if(!username||!email||!password)return res.status(400).json({error:'All fields required'});const ex=await User.findOne({$or:[{email:email.toLowerCase()},{username:username.toLowerCase()}]});if(ex)return res.status(400).json({error:ex.email===email.toLowerCase()?'Email registered':'Username taken'});const otp=genOTP();await new User({username:username.toLowerCase(),email:email.toLowerCase(),password,isVerified:false,otp:{code:otp,expiresAt:new Date(Date.now()+10*60*1000)}}).save();await sendOTP(email,otp,'verify');res.status(201).json({message:'OTP sent',email});}catch(e){res.status(500).json({error:e.message});}});
router.post('/verify-otp',async(req,res)=>{try{const{email,otp}=req.body;const user=await User.findOne({email:email.toLowerCase()});if(!user)return res.status(404).json({error:'User not found'});if(user.isVerified)return res.status(400).json({error:'Already verified'});if(!user.otp?.code||user.otp.code!==otp)return res.status(400).json({error:'Invalid OTP'});if(new Date()>user.otp.expiresAt)return res.status(400).json({error:'OTP expired'});user.isVerified=true;user.otp=undefined;const{accessToken,refreshToken}=signToks(user._id);user.refreshTokens.push(refreshToken);await user.save();setCookies(res,accessToken,refreshToken);res.json({message:'Verified',user:user.toJSON()});}catch(e){res.status(500).json({error:e.message});}});
router.post('/login',async(req,res)=>{try{const{identifier,password}=req.body;if(!identifier||!password)return res.status(400).json({error:'All fields required'});const user=await User.findOne({$or:[{email:identifier.toLowerCase()},{username:identifier.toLowerCase()}]});if(!user||!user.isVerified)return res.status(401).json({error:!user?'Invalid credentials':'Account not verified'});if(!await user.comparePassword(password))return res.status(401).json({error:'Invalid credentials'});const{accessToken,refreshToken}=signToks(user._id);if(user.refreshTokens.length>=5)user.refreshTokens.shift();user.refreshTokens.push(refreshToken);await user.save();setCookies(res,accessToken,refreshToken);res.json({message:'Login successful',user:user.toJSON()});}catch(e){res.status(500).json({error:e.message});}});
router.post('/refresh',async(req,res)=>{try{const token=req.cookies?.refreshToken;if(!token)return res.status(401).json({error:'No refresh token'});const{userId}=jwt.verify(token,process.env.JWT_REFRESH_SECRET);const user=await User.findById(userId);if(!user||!user.refreshTokens.includes(token))return res.status(401).json({error:'Invalid'});const{accessToken,refreshToken}=signToks(user._id);user.refreshTokens=user.refreshTokens.filter(t=>t!==token);user.refreshTokens.push(refreshToken);await user.save();setCookies(res,accessToken,refreshToken);res.json({user:user.toJSON()});}catch{res.status(401).json({error:'Invalid refresh token'});}});
router.post('/logout',authenticate,async(req,res)=>{try{const token=req.cookies?.refreshToken;if(token){req.user.refreshTokens=(req.user.refreshTokens||[]).filter(t=>t!==token);await req.user.save();}res.clearCookie('accessToken');res.clearCookie('refreshToken');res.json({message:'Logged out'});}catch(e){res.status(500).json({error:e.message});}});
router.get('/me',authenticate,(req,res)=>res.json({user:req.user}));
router.post('/forgot-password',async(req,res)=>{try{const{email}=req.body;const user=await User.findOne({email:email.toLowerCase()});if(!user)return res.status(404).json({error:'No account'});const otp=genOTP();user.otp={code:otp,expiresAt:new Date(Date.now()+10*60*1000)};await user.save();await sendOTP(email,otp,'reset');res.json({message:'OTP sent'});}catch(e){res.status(500).json({error:e.message});}});
router.post('/verify-reset-otp',async(req,res)=>{try{const{email,otp}=req.body;const user=await User.findOne({email:email.toLowerCase()});if(!user)return res.status(404).json({error:'Not found'});if(!user.otp?.code||user.otp.code!==otp)return res.status(400).json({error:'Invalid OTP'});if(new Date()>user.otp.expiresAt)return res.status(400).json({error:'OTP expired'});res.json({message:'OTP verified'});}catch(e){res.status(500).json({error:e.message});}});
router.post('/reset-password',async(req,res)=>{try{const{email,otp,newPassword}=req.body;const user=await User.findOne({email:email.toLowerCase()});if(!user)return res.status(404).json({error:'Not found'});if(!user.otp?.code||user.otp.code!==otp)return res.status(400).json({error:'Invalid OTP'});if(new Date()>user.otp.expiresAt)return res.status(400).json({error:'OTP expired'});user.password=newPassword;user.otp=undefined;await user.save();res.json({message:'Password reset'});}catch(e){res.status(500).json({error:e.message});}});
module.exports=router;